# Go-Scanner
The tool allows you to scan a specified host to identify open TCP ports within a defined range. It leverages Go's powerful goroutines to perform concurrent scanning, significantly speeding up the process compared to a sequential approach.
